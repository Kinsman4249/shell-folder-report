# shell-folder-report
PowerShell 5.1 script that reports the real filesystem paths of a user's Windows shell folders (Desktop, Documents, Downloads, Pictures, Music, Videos), including OneDrive Known Folder Move and network redirections.
